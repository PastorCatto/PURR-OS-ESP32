# Auto-generated by modulestrap — safe to customize.
idf_component_register(
    SRCS
        epaper_ui.c
    INCLUDE_DIRS
        .
        ../../kernel/core
        ../../kernel/catcalls
        ../../modules/user_mgr
        ../../modules/app_manager
    # user_mgr — auto-login at boot. app_manager — app_manager_notify_
    # unlocked() right after, same "any boot path that logs a session in
    # without going through a real login screen must still call this"
    # reasoning oled_ui's own CMakeLists.txt documents (this module
    # copies that exact pattern).
    REQUIRES esp_common driver freertos user_mgr app_manager
)
